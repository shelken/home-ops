# 配置：https://docs.cilium.io/en/latest/helm-reference

ipam:
  mode: kubernetes

l2announcements:
  enabled: true

kubeProxyReplacement: true
ingressController:
  enabled: true
  loadbalancerMode: shared
  default: true

