---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ooooo-space-tls
spec:
  secretName: ooooo-space-tls
  issuerRef:
    # name: letsencrypt-production
    name: letsencrypt-staging
    kind: ClusterIssuer
  commonName: ${MAIN_DOMAIN}
  dnsNames: ["${MAIN_DOMAIN}", "*.${MAIN_DOMAIN}"]