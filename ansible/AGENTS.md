## Rules

- 禁止任何 playbook文件/tasks文件 出现特别的`和节点相关的vars`配置, 节点独特配置放在inventory中对应节点, 弄成变量的方式传入
- 所有能根据环境推断出来的就不应该成为可配置的变量
- 所有变更必须幂等; 所有变更必须做到配置变化后, 有对应的行为进行承接, 例如某个配置移除, 那么就要对某个配置消失时做对应的操作.而不是出现某个配置移除之后, 配置继续残留于原先机器上
