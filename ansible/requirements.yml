# 这里定义需要安装的 collections
collections:
  - name: ansible.posix
    version: 2.0.0
  - name: ansible.utils
    version: 4.1.0
  - name: community.general
    version: 8.6.11

# 这里定义需要安装的 roles
roles:
  # PyratLabs 的 k3s role，提供更多高级特性
  - src: xanmanning.k3s
    name: xanmanning.k3s
    version: "v3.4.4"  # 指定具体版本