---
- name: 传递资源
  hosts: all
  become: true
  gather_facts: true
    # vars:
    #   k3s_proxy_ip: "192.168.6.248"
    #   k3s_proxy_port: "7890"
    #   delete_proxy: false
  tasks:
    - name: k3s registry 配置
      ansible.builtin.include_tasks: tasks/k3s-registry.yaml

    - name: sysctl 配置
      ansible.builtin.include_tasks: tasks/sysctl.yaml

    - name: 处理 longhorn multipath 可能的问题
      ansible.builtin.include_tasks: tasks/config-multipath.yaml