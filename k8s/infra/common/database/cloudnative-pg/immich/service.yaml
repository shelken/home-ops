---
apiVersion: v1
kind: Service
metadata:
  name: immich
  annotations:
    external-dns.alpha.kubernetes.io/hostname: immich-db.${MAIN_DOMAIN}
    io.cilium/lb-ipam-ips: 192.168.6.56
    gatus.home-operations.com/endpoint: |-
      group: database
      ui:
        hide-url: true
        hide-hostname: true
      conditions:
        - "[CONNECTED] == true"
      alerts:
        - type: telegram
        - type: custom
spec:
  type: LoadBalancer
  # externalTrafficPolicy: Cluster
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    cnpg.io/cluster: immich
    cnpg.io/instanceRole: primary
