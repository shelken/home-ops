---
apiVersion: v1
kind: ConfigMap
metadata:
  name: immich-cluster-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: immich-cluster
        group: database
        url: tcp://immich.database.svc.cluster.local:5432
        interval: 1m
        ui:
          hide-url: true
          hide-hostname: true
        conditions:
          - "[CONNECTED] == true"
        alerts: 
          - type: slack