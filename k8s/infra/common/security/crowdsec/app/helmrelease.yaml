---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app crowdsec
spec:
  interval: 1h
  chart:
    spec:
      chart: crowdsec
      version: 0.21.0
      sourceRef:
        kind: HelmRepository
        name: crowdsec
  # schema: https://raw.githubusercontent.com/crowdsecurity/helm-charts/refs/heads/main/charts/crowdsec/values.schema.json
  values:
    #ref: https://github.com/crowdsecurity/helm-charts/blob/main/charts/crowdsec/values.yaml
    container_runtime: containerd
    # image:
    #   repository: ghcr.io/crowdsecurity/crowdsec
    #   tag: v1.7.3
    config:
      config.yaml.local: |
        api:
          server:
            auto_registration: # Activate if not using TLS for authentication
              enabled: true
              token: "$${REGISTRATION_TOKEN}" # /!\ Do not modify this variable (auto-generated and handled by the chart)
              allowed_ranges: # /!\ Make sure to adapt to the pod IP ranges used by your cluster
                - "127.0.0.1/32"
                - "10.42.0.0/16"
                - "192.168.6.0/24"
        db_config:
          type:     postgresql
          user:     crowdsec
          password: $${DB_PASSWORD}
          db_name:  crowdsec
          host:     $${DB_HOST}
          port:     5432
          sslmode:  require
      console.yaml: |
        share_manual_decisions: true
        share_tainted: true
        share_custom: true
        console_management: true
        share_context: false

    lapi:
      extraInitContainers:
        - name: init-db
          image: ghcr.io/home-operations/postgres-init:18@sha256:866f15038ed5185a2b8118821f470bb7ca0df8c4231b8e277446e681ebb1ed84
          envFrom:
            - secretRef:
                name: crowdsec-init-db-secret
      env:
        # For an internal test, disable the Online API
        - name: DISABLE_ONLINE_API
          value: "false"
        - name: ENROLL_INSTANCE_NAME
          value: k8s
        - name: TZ
          value: ${TIMEZONE}
      envFrom:
        - secretRef:
            name: crowdsec-secret
      dashboard:
        enabled: false
      metrics:
        enabled: true
        serviceMonitor:
          enabled: true
      persistentVolume:
        # data:
        #   existingClaim: *app
        #   subPath: data
        config:
          existingClaim: *app
          subPath: config
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          memory: 512Mi
    agent:
      enabled: false
      # acquisition:
      #   - namespace: network
      #     podName: caddy-external-*
      #     program: caddy
      #   - namespace: network
      #     podName: envoy-envoy-external-*
      #     program: envoy
      # env:
      #   - name: TZ
      #     value: ${TIMEZONE}
      #   - name: COLLECTIONS
      #     value: >-
      #       crowdsecurity/linux
      #       crowdsecurity/base-http-scenarios
      #       crowdsecurity/http-cve
      #       crowdsecurity/caddy
      #   - name: PARSERS
      #     value: >-
      #       crowdsecurity/cri-logs
      #       crowdsecurity/docker-logs
      # tolerations:
      #   - key: node-role.kubernetes.io/control-plane
      #     operator: Exists
      #     effect: NoSchedule
      # metrics:
      #   enabled: true
      #   serviceMonitor:
      #     enabled: true
      # resources:
      #   requests:
      #     cpu: 50m
      #     memory: 128Mi
      #   limits:
      #     memory: 256Mi
