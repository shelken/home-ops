apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: loki
spec:
  parentRefs:
    - name: internal
      namespace: kube-system
      sectionName: https
  hostnames:
    - loki.${MAIN_DOMAIN}
  rules:
    - backendRefs:
        - name: loki-headless
          port: 3100
