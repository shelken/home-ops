---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &secret netbird
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-store
  target:
    name: netbird-secret
    template:
      data:
        SETUP_KEY: "{{ .SETUP_KEY }}"
  dataFrom:
    - extract:
        key: netbird
