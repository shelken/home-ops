---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &secret netbird-router
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-store
  target:
    name: netbird-router-secret
    template:
      data:
        SETUP_KEY: "{{ .SETUP_KEY }}"
  dataFrom:
    - extract:
        key: netbird
