---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &secret alidns-webhook
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: azure-store
  target:
    name: alidns-webhook-secret
    template:
      data:
        access-token: "{{ .secret_token }}"
        secret-key: "{{ .secret_key }}"
  dataFrom:
    - extract:
        key: alidns
