endpoints:
  - name: "${APP}"
    group: external
    url: "https://${GATUS_SUBDOMAIN:=${APP}}.ooooo.space${GATUS_PATH:=/}"
    interval: 1m
    client:
      dns-resolver: tls://223.5.5.5:853
    conditions:
      - "[STATUS] == ${GATUS_STATUS:=200}"
