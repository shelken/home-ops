---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/keda.sh/scaledobject_v1alpha1.json
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: go2rtc
spec:
  advanced:
    restoreToOriginalReplicaCount: true
  cooldownPeriod: 30
  minReplicaCount: 0
  maxReplicaCount: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: go2rtc
  triggers:
    - type: prometheus
      metadata:
        serverAddress: http://prometheus-operated.observability.svc.cluster.local:9090
        query: max(probe_success{job="probe/observability/cameras"})
        threshold: "1"
        ignoreNullValues: "0"
