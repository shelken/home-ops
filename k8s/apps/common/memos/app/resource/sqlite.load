LOAD DATABASE
	FROM sqlite:///tmp/memos_prod.db
	INTO postgresql://memos:password@postgres17.${MAIN_DOMAIN}:5432/memos?sslmode=disable
	WITH data only, disable triggers, preserve index names
set work_mem to '16MB', maintenance_work_mem to '512 MB';