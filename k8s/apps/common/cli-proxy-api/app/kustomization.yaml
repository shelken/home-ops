---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
configMapGenerator:
  - name: cli-proxy-api-configmap
    files:
      - config.yaml=./config.yaml
generatorOptions:
  disableNameSuffixHash: true
