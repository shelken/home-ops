---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app cpa-usage-keeper
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      cpa-usage-keeper:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/willxup/cpa-usage-keeper
              tag: v1.14.6@sha256:1bbb13aef2f58e74b7d6dd832bf91fc5b542939c7f7c73e848941f2d2651128c
            command: ["/app/cpa-usage-keeper"]
            env:
              TZ: ${TIMEZONE}
              WORK_DIR: /data
              CPA_BASE_URL: http://cli-proxy-api:8317
              REDIS_QUEUE_ADDR: cli-proxy-api:8317
              AUTH_ENABLED: "true"
              APP_BASE_PATH: /usage
              REDIS_QUEUE_IDLE_INTERVAL: 5s
            envFrom:
              - secretRef:
                  name: cpa-usage-keeper-secret
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 10m
                memory: 64Mi
              limits:
                memory: 512Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: &port 8080
    persistence:
      data:
        existingClaim: cpa-usage-keeper
        advancedMounts:
          cpa-usage-keeper:
            app:
              - path: /data
